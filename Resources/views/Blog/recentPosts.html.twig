<h3>Recent posts</h3>
<ul>
    {% for post in posts %}
        <li><a href="{{ path('blog_show', {'slug': post.slug, 'year': post.year, 'month': post.month }) }}" rel="bookmark" title="{{ post.title }}">{{ post.title }}</a></li>
    {% endfor %}
</ul>
